---
title: 협업과 확장
description: Git으로 변경을 공유하고 AI 변경안을 검토하며 확장 패키지의 제공 범위를 확인하는 방법.
---

# 협업과 확장

**협업과 확장**에는 Git, LLM, 플러그인과 스토어 항목이 모여 있습니다. 기본적인 세계관 작성과 로컬 플레이는 외부 서비스를 연결하지 않고 시작할 수 있습니다.

## Git — 저장한 변경을 팀에 공유하기

먼저 프로젝트를 폴더에 저장하고 제작 환경에 Git을 준비합니다.

1. 새 저장소라면 **Git init**으로 초기화합니다. 기존 팀 저장소는 [병합과 충돌](COLLABORATION.md)의 복제 안내를 따릅니다.
2. 팀 저장소의 주소로 **Origin 연결**을 설정합니다.
3. 프로젝트를 저장하고 **상태**, **변경 비교**에서 공유할 파일을 확인합니다.
4. **스테이징 경로**에 파일을 지정하고 **선택 경로 스테이징**을 누릅니다.
5. 설명할 수 있는 커밋 메시지를 입력하고 **커밋**합니다.
6. **Push**로 공유합니다. 다른 사람의 변경은 Fetch와 병합 절차로 가져옵니다.

저장은 로컬 프로젝트 파일을 기록하는 작업이고, 커밋은 Git 이력을 만드는 작업이며, Push는 원격으로 공유하는 작업입니다. 자동으로 한꺼번에 수행되지 않습니다.

브랜치 전환, 충돌 해결과 병합 취소는 [Git 팀 제작](COLLABORATION.md)에 자세히 설명합니다.

## LLM — 선택한 문맥으로 변경안 만들기

이 기능은 제작자가 준비한 호환 API의 연결 정보와 모델을 사용합니다. 실제 공급자와의 연동 결과는 환경에 따라 확인이 필요합니다.

1. **Endpoint**, 모델과 필요한 API 키를 입력합니다.
2. 문맥으로 사용할 세계관·장면 ID만 **컨텍스트 ID**에 지정합니다.
3. 요청할 내용을 적고 전송할 범위를 확인합니다.
4. 비공개·GM 정보를 포함할 때는 해당 요청의 전송 동의를 선택합니다.
5. **선택한 컨텍스트로 생성**한 후 변경안 JSON을 읽고 원하지 않는 변경을 제거합니다.
6. **검토한 변경안 적용**으로 반영한 뒤 검사하고 저장합니다.

프롬프트와 선택한 ID의 데이터가 공급자로 전송됩니다. 응답은 바로 프로젝트에 적용되지 않습니다. 생성 후 원본이 바뀌면 오래된 변경안의 적용이 차단되므로 현재 내용으로 다시 검토하세요.

## 플러그인과 스토어

데모 0.6에는 **프로젝트 확장**, **공통 허브**, **의존성·이전·신뢰**, **저장 이전**, **콘텐츠 팩 제작**이 있습니다. 확장은 선택 기능이며 기본 제작에 필수는 아닙니다.

1. **프로젝트 확장**에서 로컬 패키지를 선택하고 **패키지 검사·권한·활성화**를 엽니다.
2. 출처·해시·정확한 버전·의존성·요청 권한을 확인하고 허용할 권한을 선택해 활성화합니다.
3. **구성 요소**에서 제공되는 템플릿·편집 명령·설정·검사기를 확인합니다. 변경안은 검토한 뒤 적용합니다.
4. 게임 실행 확장은 지원되는 **게임 실행 연결**을 만들고 장면·규칙에서 호출할 위치를 확인합니다. 설치만으로 모든 행동에 적용되지는 않습니다.
5. 검사·미리보기·저장 후 같은 잠금 버전과 필요한 고지가 내보낸 게임에 포함되는지 확인합니다.

제한된 WASM 실행과 선언된 효과·상태를 공통 엔진에 연결합니다. 임의 네이티브 코드나 모든 형태의 화면 위젯을 허용하는 기능은 아닙니다. 변경·의존성 업데이트·제거·저장 이전은 적용 범위를 검토하세요. 저장 이전은 정확한 호환 조건을 만족할 때 별도 사본을 만드는 도구이며 모든 엔진 버전의 자동 변환이 아닙니다.

콘텐츠 팩은 지원되는 정의와 관리 이미지를 출처 정보와 함께 선택합니다. **공통 허브** 화면은 지정한 서비스에 접속합니다. 자체 운영 가능한 허브와 공식 공개 카탈로그의 운영은 별개이며, 실제 Steam 창작마당 구독과 Steam·STOVE·Epic 서비스 연동이 완료된 것은 아닙니다. 공식 제품 페이지는 [스토어 공개 예정](DEVELOPMENT.md#stores)입니다.

## 앞으로의 작업 위치

플러그인의 프로젝트 정책과 Git은 [프로젝트 설정](DESIGN.md)으로 모으고, 제작 내용과 선택형 LLM 보조는 편집 대상 가까이 두는 방향입니다. 현재 메뉴가 새 설정 화면으로 이미 이동한 것은 아닙니다.

## 작업 후 확인

- 공유하려는 파일만 스테이징했는지 확인합니다.
- AI 변경안의 대상과 수정 내용이 의도와 맞는지 검사합니다.
- 확장 시험 결과를 정식 제품 지원이나 전체 게임 호환성으로 해석하지 않습니다.
- 팀 변경이나 제안을 적용한 뒤 실제 플레이 경로를 다시 확인합니다.

**관련 안내:** [Git 팀 제작](COLLABORATION.md) · [번역 작업실](LOCALIZATION.md) · [개발 상태](DEVELOPMENT.md)

## 같은 원본을 바꾸는 여러 방법

직접 편집·템플릿·LLM 변경안·Git 병합이 같은 원본과 참조를 유지하고, 일부만 적용해도 다시 검사하는 [제작·시험 연결 목표](TEST_AND_RELEASE.md)를 준비합니다. LLM은 선택 사항이며 제안이 세계의 확정 사실이나 실행 결과를 자동 변경하지 않습니다.
